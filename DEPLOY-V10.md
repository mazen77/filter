# MAZ LAB 10

Extract the ZIP and upload its contents, including vendor, studio.js and studio.css.
Replace matching application files; DO NOT delete your repository, CNAME or unrelated files.
Keep the GitHub Pages custom domain set to filter.fa7ma.com and enable Enforce HTTPS.
Visit https://filter.fa7ma.com/ directly after deployment.
The home screen should say version 10. If not, the old deployment is still being served.

Camera frames stay on the device. File-mode testing needs internet for model assets.
Hosted mode attempts bundled assets first, then a pinned CDN fallback.
Camera permission and a secure browser context are required. No code bypasses these.
Full-screen filters, random selection and meme captions work independently of hand tracking.
Choose hand portal mode for the original two-hand interaction.

Verification: JavaScript syntax and archive/static-reference checks only. Live human-hand
tracking, device-specific camera behavior and recording require testing on real devices.
The supplied update has not been deployed to your domain by the assistant.
